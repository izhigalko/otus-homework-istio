apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw04-istio-chart.fullname" . }}
  labels:
    {{- include "hw04-istio-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "hw04-istio-chart.selectorLabels" . | nindent 4 }}

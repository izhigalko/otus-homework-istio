apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hw04-istio-chart.fullname" . }}-test-connection"
  labels:
    {{- include "hw04-istio-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hw04-istio-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

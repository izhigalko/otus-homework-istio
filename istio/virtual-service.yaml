apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: virtual-service
spec:
  hosts:
    - "*"
  gateways:
    - gateway
  http:
    - match:
        - uri:
            exact: /health
      route:
        - destination:
            host: service.default.svc.cluster.local
            port:
              number: 8000
            subset: v1
          weight: 50
        - destination:
            host: service
            port:
              number: 8000
            subset: v2
          weight: 50
---
apiVersion: v1
kind: Namespace
metadata:
  name: default
  labels:
    istio-injection: enabled # Включаем инжект сайдкаров Istio для неймспейса

---
apiVersion: v1
kind: Service
metadata:
  name: tasks-service
  namespace: default
spec:
  type: NodePort
  ports:
    - port: 80
      nodePort: 32085
      targetPort: 5000
      name: "http"  # Istio требует явного указания протокола при определенных настройках
  selector:
    app: tasks-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tasks-service
  namespace: default
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tasks-service
      version: "1"
  template:
    metadata:
      labels:
        app: tasks-service
        version: "1"
    spec:
      containers:
        - name: tasks-service
          image: vladraven/otus_istio_tasks_service
          imagePullPolicy: Always
          ports:
            - containerPort: 5000

---

# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: tasks-service
#   labels: 
#     app: tasks-service
#     version: "1"
# spec:
#   jobLabel: tasks-service
#   namespaceSelector:
#     matchNames:
#     - default
#   selector:
#     matchLabels:
#       app: tasks-service
#       version: "1"
#   endpoints:
#   - interval: 15s
#     port: web
#     path: /metrics